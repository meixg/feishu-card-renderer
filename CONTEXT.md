# 飞书卡片 JSON 2.0 Web 渲染器

本上下文定义宿主无关的飞书卡片 JSON 2.0 Web 渲染语义，以及协议输入在浏览器中的安全、稳定呈现边界。

## Language

**富文本升级**:
将 `markdown` 内容解析为安全的语义结构，并以适合卡片场景的完整排版体系呈现；它同时包含语义正确性和视觉层级，不等同于只调整 CSS。
_Avoid_: Markdown 美化、样式微调

**卡片原生排版**:
服务于紧凑卡片阅读的克制型富文本视觉语言，强调清晰层级、舒适间距和低干扰装饰，而不是模拟长篇博客或文档页面。
_Avoid_: 博客风、文档站风、重装饰排版

**安全 Markdown 语义集**:
当前明确支持的 CommonMark 块级与行内结构；原始 HTML 不执行、不直接注入 DOM，飞书扩展标签只在其协议语义被单独实现后进入该集合。
_Avoid_: 完整 HTML、浏览器原生 HTML 透传

**宿主主题变量**:
由集成方覆盖的稳定 `--fcr-*` 语义样式入口，用于品牌适配；其默认值来自 shadcn/ui，并在内部映射到当前 shadcn 快照的主题 token。
_Avoid_: Markdown 样式 props、JSON 自定义 CSS、公开 shadcn 内部 token

**卡片配色模式**:
由每个 `CardRenderer` 显式选择的 light 或 dark 呈现环境；它独立于宿主页面的全局主题并同时作用于该卡片的 portal 内容。
_Avoid_: 全局 dark class、隐式系统主题

**交互视图**:
承载用户输入、选择、确认和浮层操作的内部视图；其视觉采用 shadcn/ui，协议行为仍由渲染器定义。
_Avoid_: 飞书风格控件、协议组件样式

**shadcn 默认视觉**:
以 shadcn/ui `base-nova` 生成源码的结构、样式类和主题 token 为交互视图的视觉事实来源；renderer 只做宿主作用域、类名前缀、portal 和打包适配。
_Avoid_: 飞书控件仿制、renderer 自创控件主题

**功能对齐**:
交互视图完整保留飞书 JSON 2.0 的值、表单、动作和上下文约束；shadcn/ui 提供视觉与可访问交互机制，但不改变协议语义。
_Avoid_: 飞书视觉复刻、shadcn 示例行为照搬

**协议视觉语义**:
协议中用于区分操作重要性、危险性和控件状态的视觉含义；它映射到最接近的 shadcn/ui variant，不承诺飞书的具体颜色或像素效果。
_Avoid_: 飞书主题复刻、自定义控件配色

**交互集成布局**:
使 shadcn/ui 交互视图适应卡片宽度、内容溢出、portal 层级和移动设备的布局约束；它不定义颜色、边框、圆角、阴影或交互状态外观。
_Avoid_: 控件主题、飞书样式覆盖

**系统文案**:
由 renderer 为搜索、空状态、确认、关闭和无障碍提示补充的本地化文本；它由 `locale` 决定，不继承 shadcn 示例文案。
_Avoid_: wrapper 硬编码文案、shadcn 示例文案

**协议内容布局**:
将容器、展示元素和富文本等飞书 JSON 2.0 结构排布为可读卡片的宿主无关布局；不由 shadcn/ui 定义。
_Avoid_: shadcn 页面布局、交互控件布局

**元素流**:
由 `body`、`column`、`form`、`interactive_container` 或 `collapsible_panel` 拥有的纵向有序兄弟协议组件序列；父级只负责相邻兄弟之间的默认间距，容器边缘仍由 `padding` 管理，元素自身的协议外边距作为额外偏移与父级间距相加。嵌套元素流各自管理自己的兄弟序列，不在父子边界重复计算间距；顶层 `header`、横向分栏、横向排列，以及组件内部的 label、control、feedback 和 overlay 不属于元素流。
_Avoid_: 元素配对间距、组件自带默认外边距

**默认元素流间距**:
元素流未显式声明间距或声明值非法时采用的紧凑卡片基础节奏，对应 `medium`（8px），且不因 PC/mobile 设备模式改变；合法的 `0px` 表示显式关闭父级间距。
_Avoid_: 零间距、按元素类型猜测间距

**布局参与节点**:
一个协议 `CardElement` 在父元素流中对应的唯一文档流节点；组件内部反馈节点和通过 portal 呈现的浮层不额外占用元素流位置。
_Avoid_: DOM 子节点计数、portal 间距

**可见原文降级**:
未知原始 HTML 或尚未实现的飞书扩展标签以转义后的原文呈现，保持内容可读且不可执行；该行为是渲染器的稳定安全策略，不冒充未被官方定义的客户端行为。
_Avoid_: 静默删除、猜测性标签解释

**有界富文本**:
解析成本受字符数、结构深度和复杂集合规模限制的富文本；超限时保留安全前缀、显示截断提示并产生可恢复诊断，不使整卡失败。
_Avoid_: 无提示截断、整卡失败
